//fuction keyword --declare the function
//function declaration also gets hoisted
//add --function name
//a, b --parameters
//return--keyword to return value from a function, if we dont return any value from a function, by default undefined gets returned
//parameter--are also called as local varibles inside the function

function add(a, b) {
  //parameter (a, b)
  return a + b;
}

console.log(add(1, 3)); //4     //arguments (1, 3)
//10 is assigned to a and 20 is assigned to b
